/**
 * ตัวตนของ "แอป" — ชื่อและเลขเวอร์ชัน ที่ทุกหน้าจอต้องแสดงให้ตรงกัน
 *
 * ⚠️ คนละเรื่องกับ "องค์กร" ที่ตั้งค่าเองได้จากหน้าตั้งค่าองค์กร (ชื่อบริษัท/โลโก้/ที่อยู่)
 *    ค่าที่นี่คือของตัวซอฟต์แวร์ ไม่เปลี่ยนตามองค์กรที่เอาไปใช้
 * ⚠️ เลขเวอร์ชันมาจาก "version" ใน package.json ที่เดียว ผ่านโมดูลเสมือนที่ตั้งไว้ใน vite.config.js
 *    ขึ้นเวอร์ชันด้วย `npm version patch|minor|major` แล้ว build ใหม่ อย่าไปพิมพ์เลขไว้ในโค้ด
 * ⚠️ ชื่อแอปยังต้องไปแก้ที่ index.html (<title>) และ public/manifest.json ด้วยถ้าเปลี่ยน —
 *    สองไฟล์นั้นเป็นไฟล์สแตติก อ่านค่าจากโมดูลนี้ไม่ได้
 */

/** ชื่อแอปที่แสดงให้ผู้ใช้เห็น */
export const APP_NAME = "WiRix";

/** เลขเวอร์ชันจาก package.json (ดูปลั๊กอิน app-version ใน vite.config.js) */
export { default as APP_VERSION } from "virtual:app-version";
